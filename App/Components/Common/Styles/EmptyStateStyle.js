import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    flex: 1,
  },
  image: {
    resizeMode: 'contain',
    height: 200,
    width: 200
  },
  button: {
    marginTop: 30
  }
})
